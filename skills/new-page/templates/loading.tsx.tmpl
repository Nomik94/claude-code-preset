/**
 * {PageName} 로딩 UI
 *
 * Suspense 경계에서 자동으로 표시된다.
 * 실제 페이지 레이아웃과 동일한 구조의 스켈레톤을 제공한다.
 */
export default function {PageName}Loading() {
  return (
    <main
      className="container mx-auto px-4 py-8 animate-pulse"
      aria-label="페이지 로딩 중"
      aria-busy="true"
    >
      {/* 제목 스켈레톤 */}
      <div className="mb-6 h-9 w-48 rounded-md bg-gray-200" />

      {/* 콘텐츠 스켈레톤 */}
      <section className="space-y-4" aria-label="콘텐츠 로딩 중">
        <div className="h-4 w-full rounded bg-gray-200" />
        <div className="h-4 w-5/6 rounded bg-gray-200" />
        <div className="h-4 w-4/6 rounded bg-gray-200" />

        {/* 카드 스켈레톤 */}
        <div className="mt-8 grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">
          {Array.from({ length: 3 }, (_, i) => (
            <div
              key={i}
              className="h-32 rounded-lg bg-gray-200"
            />
          ))}
        </div>
      </section>
    </main>
  );
}
