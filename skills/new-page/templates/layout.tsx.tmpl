import type { Metadata } from "next";

export const metadata: Metadata = {
  title: {
    template: `%s | {PageName}`,
    default: "{PageName}",
  },
  description: "{description}",
};

interface {PageName}LayoutProps {
  children: React.ReactNode;
}

/**
 * {PageName} 레이아웃
 *
 * 이 라우트 그룹의 공통 레이아웃을 정의한다.
 * 네비게이션, 사이드바 등 공유 UI를 배치한다.
 */
export default function {PageName}Layout({ children }: {PageName}LayoutProps) {
  return (
    <div className="flex min-h-screen flex-col">
      {/* TODO: 필요 시 라우트 전용 네비게이션 추가 */}
      <nav
        className="border-b bg-white px-4 py-3"
        aria-label="{PageName} 네비게이션"
      >
        <div className="container mx-auto">
          <h2 className="text-lg font-semibold">{PageName}</h2>
        </div>
      </nav>

      {/* 메인 콘텐츠 */}
      <div className="flex-1">{children}</div>
    </div>
  );
}
